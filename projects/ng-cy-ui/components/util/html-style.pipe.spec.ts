import { HtmlStylePipe } from './html-style.pipe';

describe('HtmlStylePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlStylePipe();
    expect(pipe).toBeTruthy();
  });
});
